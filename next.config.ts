import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'via.placeholder.com',
      'img.antaranews.com',
      "klimg.com",
      "pict.sindonews.net"
    ],
  },
};

export default nextConfig;
